/* let boton = document.getElementById("mostrar-menu");
boton.addEventListener("click", mostrarMenu);

function mostrarMenu() {
  boton.style.display = "none";
}
 */
/* const button = document.querySelector(".mostrar-menu");
const icon = document.querySelector(".fa-bars");

icon.style.transform = "300ms ease-on-out";
let open = false;

button.addEventListener("click", () => {
  if (!open) {
    icon.style.transform = rotate(-90deg);
    setTimeout(() => icon.setAttribute("class", "fas fa-times icon"), 120);
    open = true;
  } else {
    icon.style.transform = rotate(0deg);
    setTimeout(() => icon.setAttribute("class", "fas fa-bars"), 120);
    open = false;
  }
}); */
